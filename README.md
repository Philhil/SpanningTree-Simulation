# SpanningTree-Simulation

Compile: make

Compile with debug: make debug

run Simulation: ./spantree datafile

run Simulation with Debug output: ./spantree_debug datafile

#### Datafile Format
Graph `graphname` {

`node` = `id`;

//`comment`

`node` - `node` : `linkcosts`;

}
